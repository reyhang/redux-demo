import React from 'react'

const Header =() => {
    return(
        <header style={{marginBottom:20}}>
            <h1> Header </h1>
            <button> Gece Modunu Değiştir </button>
        </header>
     )
}

export default Header;